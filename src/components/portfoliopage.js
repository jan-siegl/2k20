import React, {Component} from "react";

export default class Portfoliopage extends Component {
    render() {
        return(
            <div>
                pleb
            </div>
        )
    }
}